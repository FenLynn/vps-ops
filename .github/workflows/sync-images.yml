name: 📦 手动同步 Docker 镜像至 GHCR

# 仅允许手动触发
on:
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  # 将拥有者名称转换为小写 (GHCR 强制要求小写)
  IMAGE_OWNER: ${{ github.repository_owner }}

jobs:
  sync-images:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write # 必须的权限：允许 GITHUB_TOKEN 推送镜像到 GHCR

    steps:
      - name: 1. 将拥有者名称转为小写 (处理大写用户名问题)
        run: echo "IMAGE_OWNER=$(echo ${{ env.IMAGE_OWNER }} | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: 2. 登录到 GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 3. 中转 Kopia 镜像
        run: |
          docker pull kopia/kopia:latest
          docker tag kopia/kopia:latest ${{ env.REGISTRY }}/${{ env.IMAGE_OWNER }}/kopia:latest
          docker push ${{ env.REGISTRY }}/${{ env.IMAGE_OWNER }}/kopia:latest

      - name: 4. 中转 Unblock-Netease 镜像
        run: |
          docker pull pan93412/unblock-netease-music-enhanced:latest
          docker tag pan93412/unblock-netease-music-enhanced:latest ${{ env.REGISTRY }}/${{ env.IMAGE_OWNER }}/unblock-netease-music-enhanced:latest
          docker push ${{ env.REGISTRY }}/${{ env.IMAGE_OWNER }}/unblock-netease-music-enhanced:latest

      - name: 5. 中转 Derper 镜像
        run: |
          docker pull fredliang/derper:latest
          docker tag fredliang/derper:latest ${{ env.REGISTRY }}/${{ env.IMAGE_OWNER }}/derper:latest
          docker push ${{ env.REGISTRY }}/${{ env.IMAGE_OWNER }}/derper:latest

      - name: 6. 自动将容器镜像设为公开 (Public)
        env:
          # 优先使用用户自定义的 GH_TOKEN，如果没有则回退回 github.token 避免 gh CLI 报错
          GH_TOKEN: ${{ secrets.GH_TOKEN || github.token }}
        run: |
          echo "🌐 正在将镜像可见性设置为公开..."
          
          # 检查 Token 是否看起来像是通过 Secrets 传入的 (长度过短可能代表 Secret 没配对)
          if [ "${#GH_TOKEN}" -lt 20 ]; then
             echo "⚠️ 警告: 发现 GH_TOKEN 异常短或未设置，可能无法修改可见性权限。"
          fi

          for pkg in kopia unblock-netease-music-enhanced derper; do
            echo "  - 尝试设置 ${pkg} 为公开..."
            # 使用更通用的 API 路径：/orgs/{org} 或 /users/{user}
            # 注意：GHCR API 路径通常是小写的
            gh api \
              --method PATCH \
              -H "Accept: application/vnd.github+json" \
              -H "X-GitHub-Api-Version: 2022-11-28" \
              "/user/packages/container/${pkg}/manage_visibility" \
              -f visibility='public' || \
            gh api \
              --method PATCH \
              -H "Accept: application/vnd.github+json" \
              -H "X-GitHub-Api-Version: 2022-11-28" \
              "/orgs/${{ env.IMAGE_OWNER }}/packages/container/${pkg}/manage_visibility" \
              -f visibility='public' || \
            echo "  ❌ ${pkg} 公开权限自动设置失败。原因：GH_TOKEN 权限不足或 Package 不在当前账号下。"
          done
          
          echo "💡 如果自动设置失败，请手动：GitHub -> Packages -> Package Settings -> Change Visibility -> Public"
