name: Sync Docker Hub Images to GHCR

on:
  workflow_dispatch:
  schedule:
    - cron: '0 4 * * 1' # 每周一自动向你的 GHCR 兜底同步更新一次

env:
  REGISTRY: ghcr.io
  # GitHub 强制要求仓库 namespace 为全小写
  OWNER: fenlynn

jobs:
  sync-to-ghcr:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: 1. Log in to the GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
          
      - name: 2. Pull, Retag and Push Images
        run: |
          # 注意：以下列表必须只包含从外界 Docker Hub 抓取的“他源”镜像！
          # 对于本来就在你 GitHub 自动构建的（如 derper, kopia, unblock-netease），不要放入此表！
          IMAGES=(
            "docker.io/qier222/yesplaymusic:latest|yesplaymusic:latest"
            "docker.io/cloudflare/cloudflared:latest|cloudflared:latest"
            "docker.io/neilpang/acme.sh:latest|acme.sh:latest"
            "docker.io/louislam/uptime-kuma:1|uptime-kuma:1"
            "docker.io/louislam/dockge:1|dockge:1"
            "docker.io/library/nginx:alpine|nginx:alpine"
            "docker.io/calciumion/new-api:latest|new-api:latest"
            "docker.io/binaryify/netease_cloud_music_api:latest|netease_cloud_music_api:latest"
          )
          
          for item in "${IMAGES[@]}"; do
            SOURCE_IMG="${item%%|*}"
            TARGET_IMG_NAME="${item##*|}"
            TARGET_IMG="${{ env.REGISTRY }}/${{ env.OWNER }}/$TARGET_IMG_NAME"
            
            # 防御性过滤：如果配置项本身就是 ghcr.io 开头的官方仓来源，则直接跳过！防止套娃
            if [[ "$SOURCE_IMG" == ghcr.io/* ]]; then
              echo "⏭️ 跳过原生 GHCR 镜像: $SOURCE_IMG"
              continue
            fi
            echo "----------------------------------------"
            echo "🚀 智能同步: $SOURCE_IMG -> $TARGET_IMG"
            
            # 使用 skopeo 代替 docker pull/push
            # --all 会原封不动地拷贝多架构 (Multi-arch) 的所有底层架构层
            # skopeo 最大的特点是：如果目标仓库层的指纹 (Digest) 与源仓库相同，它会瞬间秒跳过，绝不重复下载/上传！
            skopeo copy --all docker://$SOURCE_IMG docker://$TARGET_IMG --dest-creds ${{ github.actor }}:${{ secrets.GITHUB_TOKEN }}
            
            echo "✅ $TARGET_IMG 处理结束"
          done

      - name: 3. Trigger VPS to Pull and Update
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.VPS_HOST }}
          username: root
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            echo "1. 触发 VPS 从刚刚同步好的 GHCR 拉取全量更新..."
            cd /opt/vps-dmz/compose
            docker-compose pull
            
            echo "2. 重启改变的容器并移除孤儿..."
            docker-compose up -d --remove-orphans
            
            echo "3. 清理 VPS 旧镜像..."
            docker image prune -f
            
            echo "🎉 GHCR 的转存与拉取更新全面完成！"
