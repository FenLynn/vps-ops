name: 📦 手动同步 Docker 镜像至 GHCR

# 仅允许手动触发
on:
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  # 将拥有者名称转换为小写 (GHCR 强制要求小写)
  IMAGE_OWNER: ${{ github.repository_owner }}

jobs:
  sync-images:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write # 必须的权限：允许 GITHUB_TOKEN 推送镜像到 GHCR

    steps:
      - name: 1. 将拥有者名称转为小写 (处理大写用户名问题)
        run: echo "IMAGE_OWNER=$(echo ${{ env.IMAGE_OWNER }} | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: 2. 登录到 GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 3. 拉取并中转 Kopia 镜像
        run: |
          docker pull kopia/kopia:latest
          docker tag kopia/kopia:latest ${{ env.REGISTRY }}/${{ env.IMAGE_OWNER }}/kopia:latest
          docker push ${{ env.REGISTRY }}/${{ env.IMAGE_OWNER }}/kopia:latest

      - name: 4. 自动将容器镜像设为公开 (Public)
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          echo "🌐 正在将镜像可见性设置为公开..."
          # 注意：此操作需要 GH_TOKEN 具备 admin:packages 权限
          gh api \
            --method PATCH \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "/user/packages/container/kopia/manage_visibility" \
            -f visibility='public' || echo "⚠️ 自动设置公开权限失败，请检查 GH_TOKEN 权限是否包含 admin:packages"
