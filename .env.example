# Credentials for vps-ops
# Rename this file to .env and fill in the values

# --- System ---
# Password for the sudo user 'sudor' (for sudo access if needed, though NOPASSWD is set)
ADMIN_PASS=change_me_to_a_secure_password

# --- Cloudflare ---
# Tunnel Token (from Zero Trust Dashboard)
CF_TOKEN=eyJh...
# DNS API Token for acme.sh to generate wildcards
CF_DNS_API_TOKEN=...
# Account ID (Optional, used by some scripts)
CLOUDFLARE_ACCOUNT_ID=...

# --- Notifications ---
# WeChat Notification Token
PUSHPLUS_TOKEN=...

# --- Tailscale ---
# Auth Key for the host client
TAILSCALE_AUTH_KEY=tskey-...

# --- Derp ---
# Domain for Derper (used by docker-compose)
DERP_DOMAIN=derp.yourdomain.com

# --- GitHub / GHCR ---
# Personal Access Token (PAT) for GHCR login
GH_TOKEN=ghp_your_token_here
